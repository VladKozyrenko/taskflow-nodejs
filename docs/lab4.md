# Лабораторна робота №4

## Імплементація інтеграції з віддаленими джерелами даних

**Дисципліна:** Розробка програмного забезпечення на платформі Node.js

---

## 🎯 Мета роботи

Метою лабораторної роботи є інтеграція застосунку з реальним віддаленим джерелом даних
та заміна статичних in-memory даних на повноцінну роботу з базою даних.

---

## 🧩 Опис реалізованого рішення

У рамках лабораторної роботи було реалізовано підключення до реляційної бази даних
**PostgreSQL** з використанням ORM **Prisma**.

Архітектура застосунку була змінена таким чином, що всі основні сценарії роботи з даними
виконуються безпосередньо через базу даних, а не через статичні масиви в памʼяті.

---

## 🛠️ Використані технології

- **Node.js**
- **TypeScript**
- **Express**
- **PostgreSQL**
- **Prisma ORM**
- **Docker**
- **Prisma Studio**

---

## 🗄️ Модель даних

Було реалізовано такі сутності:

### User

- id (PK)
- email

### Project

- id (PK)
- name
- ownerId (FK → User)

### Task

- id (PK)
- title
- description
- status
- projectId (FK → Project)

Між сутностями реалізовано звʼязки:

- User → Project (1:N)
- Project → Task (1:N)

---

## 🔗 Інтеграція з базою даних

### Підключення БД

База даних PostgreSQL була розгорнута за допомогою Docker-контейнера.
Параметри підключення зберігаються у змінних середовища (`.env`).

### Prisma

Для роботи з базою даних використовується Prisma ORM:

- опис схеми — `prisma/schema.prisma`
- міграції — `prisma/migrations`
- генерація клієнта — `@prisma/client`

---

## 🔁 Заміна статичних даних

У лабораторній роботі №3 використовувались статичні масиви даних
(`src/data/*.ts`), які імітували роботу бази даних.

У Lab 4:

- статичні дані були повністю видалені
- усі HTTP-роутери переведені на роботу через Prisma

### Приклад:

**Було (Lab 3):**

```ts
res.json(users);
```

**Стало (Lab 4):**

```ts
const users = await prisma.user.findMany();
res.json(users);
```

## 🌐 Реалізовані HTTP-ендпоінти

- GET /users — отримання користувачів з БД
- GET /projects — отримання проєктів з БД
- GET /tasks — отримання задач з БД
  Усі запити повертають реальні дані з PostgreSQL.

## 🌱 Seed-дані

Для наповнення бази тестовими даними було реалізовано seed-скрипт:

- файл: src/seed.ts
- запуск: npm run seed

Seed створює користувача, проєкт та задачі, що дозволяє швидко перевірити роботу системи.

## ✅ Висновки

У результаті виконання лабораторної роботи було:

- інтегровано реальну базу даних PostgreSQL
- реалізовано ORM-шар за допомогою Prisma
- замінено статичні дані на повноцінну роботу з БД
- забезпечено масштабовану та розширювану архітектуру застосунку

Отримане рішення повністю відповідає вимогам лабораторної роботи №4.
